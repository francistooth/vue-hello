const { createApp } = Vue

createApp({
   data() {
      // return {
      //    message: 'hello vue!!'
      // }
   },
   methods: {
      inputSubmit: function() {
         return {
            message: 'accipicchia'
         }
      }
   }
}).mount('#app');